#modeb(cause_harm_to(var(nn),var(nn))).
#modeb(be(var(nn),var(nn))).
#modeh(cause_harm_to(var(nn),var(nn))).
#maxv(3)
#max_penalty(50)
#pos({cause_harm_to(posion,tree)},
{
cause_harm_to(poison,robot),
cause_harm_to(poison,house),
cause_harm_to(poison,car)
},
{be(tree,plant).
:-be(robot,plant).
be(plant,live_thing).
cause_harm_to(poison,live_thing).
}
).