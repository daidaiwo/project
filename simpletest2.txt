#modeb(be_responsible_for(var(nn),var(nn))).
#modeb(be(var(nn),var(nn))).
#modeh(be_responsible_for(var(nn),var(nn))).
#modeb(produce(var(nn),var(nn))).
#modeb(need(var(nn),var(nn))).
#maxv(3).
#max_penalty(50).
#pos({be_responsible_for(sun,tree)},
{
},
{
be_responsible_for(sun,plant).
be(tree,plant).
}
).
#pos({be_responsible_for(sun,man)},
{
},
{produce(sun,light).
need(man,light).
}
).